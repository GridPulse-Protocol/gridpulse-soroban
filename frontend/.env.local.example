# Base URL of the GridPulse backend (the Fastify relayer/API).
NEXT_PUBLIC_API_URL=http://localhost:4000

# The Stellar network the frontend should target for wallet interactions.
# Must match the backend's NETWORK. One of: testnet | mainnet.
NEXT_PUBLIC_NETWORK=testnet
